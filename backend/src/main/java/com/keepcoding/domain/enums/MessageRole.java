package com.keepcoding.domain.enums;

/** Papel de uma mensagem dentro do diálogo de entrevista. */
public enum MessageRole {
    /** Mensagem do entrevistador (IA). */
    INTERVIEWER,
    /** Mensagem do candidato (usuário). */
    CANDIDATE
}
